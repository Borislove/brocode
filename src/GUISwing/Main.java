package GUISwing;

public class Main {
    public static void main(String[] args) {
        // JFrame = a GUI window to add components to
     /*   JFrame frame = new JFrame(); //creates a frame
        frame.setTitle("JFrame title goes here"); //sets title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application
        //frame.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE); // не закрывать при нажатии на крестик
        frame.setResizable(false); //prevent frame from being resized
        frame.setSize(400, 400); //sets the x-dimension, and y-dimension of frame
        frame.setVisible(true); //make frame visible

        ImageIcon image = new ImageIcon("logo.png"); //create an ImageIcon
        frame.setIconImage(image.getImage()); //change icon of frame
        // frame.getContentPane().setBackground(Color.gray); //change color of background
        //  frame.getContentPane().setBackground(new Color(0, 0, 0));
        frame.getContentPane().setBackground(new Color(0x837da2)); //hex color
        frame.setLocationRelativeTo(null); //center*/

        // MyFrame myFrame = new MyFrame();
        new MyFrame();
    }
}
